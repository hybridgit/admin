<% @user.user_roles.each do |user_role| %>
  <tr >
    <td><%= user_role.role.name %></td>
    <td class="actions right-align">
      <%= link_to raw("<i class='material-icons circle'>cancel</i>"), user_role_delete_path(@user, user_role), remote: true %>
    </td>
  </tr>
<% end %>
